# for system
alias ll='ls -l'
alias cp='cp -i'

# alias for git
alias ga="git add"
alias gm="git commit"
alias gma="git commit --amend"
alias gb="git branch -vv"
alias gba="git branch --all"
alias gd="git diff --color"
alias gdc="git diff --color --cached"
alias gl="git log --graph --stat"
alias gps="git push"
alias gpl="git pull"
alias gplr="git pull --rebase"
alias grc="git rebase --continue"
alias gst="git status"
alias gs="git show"
alias cmx="chmod +x"
alias gco="git checkout"
alias gr="git remote -v"
alias gls="git log -1 -p HEAD"
alias gmls="git commit -c HEAD"
alias glm="git log -n1 -p"
alias gf="git fetch"
alias grh="git reset --hard"
alias gcf="git clean -f"
alias gcp="git cherry-pick"
alias gsh="git stash"
alias gsp="git stash pop"
alias gbd="git branch -D"

# alias for rails
alias rco="rails c"
alias rcos="rails c --sandbox"
alias rss="rails s"

# alias for linux command
alias cls="clear"
alias v="vim"
alias cp='cp -i'
alias rm='rm -i'
alias chx="chmod +x"
alias vg="vim ~/.bashrc-git"
alias vmac="vim ~/.bashrc-mac"
alias vz="vim ~/.zshrc"
alias vv="vim ~/.vimrc"
alias f="find . -iname"
alias h="history"
alias s="screen"
alias condb="mysql --default-character-set=utf8 -uroot -hmysql.lo.mixi.jp"
alias prove="/usr/local/mixi-devtools/mixi-test/bin/mixi-prove"
alias mc="/home/bkapps/git/touch-skin/script/devel/service_activation_per_user"
alias agi="ag -i"
alias agl="ag '--pager=less -R'"
alias agli="ag --ignore log --ignore coverage '--pager=less -R'"
alias jl="jobs -l"

# chrome
alias openchrome="open -a Google\ Chrome"

# add ssh
alias ssh-siau="ssh feng.zhang@siau -i ~/.ssh/detroit/id_rsa -A"
alias ssh-stg="ssh -Ap 2222 kyoro@stg.deploygate.com"
alias ssh-dg-real="ssh -Ap 2222 kyoro@deploygate.com"
alias ssh-ec2="ssh -A fengzhang@54.92.65.144"

# fout
alias ssh-dev="ssh -i ~/fout/aws/config/feng_dev_key_pair.pem  ec2-user@ec2-52-68-90-196.ap-northeast-1.compute.amazonaws.com"
alias ssh-foryou2="ssh -i .ssh/foryou-production.pem ec2-user@52.69.127.19"
alias ssh-foryou="ssh -i .ssh/foryou-production.pem ec2-user@54.64.37.0"

# add for ruby
alias r="/usr/bin/ruby"

# rails and sidekiq
alias restart_rails='cd /Users/feng.zhang/home/deploygate-app/deploygate/webfront; kill -9 `cat tmp/pids/server.pid`; script/sslrails s -d'
alias restart_sidekiq='cd /Users/feng.zhang/home/deploygate-app/deploygate/webfront; sidekiqctl stop tmp/pids/sidekiq.pid 60; bundle exec sidekiq -C config/sidekiq.yml -d'

# Release Ctrl-Q and Ctrl-S from tty
stty stop undef
stty start undef

# for vagrant
alias envv='envchain aws bash'
alias envstart="envchain aws bash -c 'vagrant up --provider aws; vagrant gatling-rsync-auto'"
alias vagrant_rsync='vagrant rsync'
alias vagrant_s='vagrant gatling-rsync-auto'
