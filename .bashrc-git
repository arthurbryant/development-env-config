# for system
alias ll='ls -l'
alias cp='cp -i'
alias grep='grep --color'

# alias for git
alias ga="git add"
alias gm="git commit"
alias gma="git commit --amend"
alias gb="git branch -vv"
alias gba="git branch --all"
alias gd="git diff --color"
alias gdc="git diff --color --cached"
alias grh="git reset HEAD"
alias gl="git log --graph --stat"
alias gps="git push"
alias gpl="git pull"
alias gplr="git pull --rebase"
alias grc="git rebase --continue"
alias gst="git status"
alias gs="git show"
alias cmx="chmod +x"
alias gco="git checkout"
alias gr="git remote -v"
alias gls="git log -1 -p HEAD"
alias gmls="git commit -c HEAD"
alias glm="git log -n1 -p"
alias gf="git fetch"
alias gcf="git clean -f"
alias gcp="git cherry-pick"
alias gsh="git stash"
alias gsp="git stash pop"
alias gbd="git branch -D"

# alias for rails
alias rco="rails c"
alias rcos="rails c --sandbox"
alias rss="rails s"

# alias for linux command
alias cls="clear"
alias v="vim"
alias cp='cp -i'
alias rm='rm -i'
alias chx="chmod +x"
alias vg="vim ~/.bashrc-git"
alias vmac="vim ~/.bashrc-mac"
alias vz="vim ~/.zshrc"
alias vv="vim ~/.vimrc"
alias f="find . -iname"
alias h="history"
alias s="screen"
alias condb="mysql --default-character-set=utf8 -uroot -hmysql.lo.mixi.jp"
alias prove="/usr/local/mixi-devtools/mixi-test/bin/mixi-prove"
alias mc="/home/bkapps/git/touch-skin/script/devel/service_activation_per_user"
alias agi="ag -i"
alias agl="ag '--pager=less -R'"
alias agli="ag --ignore log --ignore coverage '--pager=less -R'"
alias jl="jobs -l"

# chrome
alias openchrome="open -a Google\ Chrome"

# fout
alias ssh-dev="ssh -A ec2-user@dev.foru.club"
alias ssh-stg="ssh -A ec2-user@52.68.95.181"
alias ssh-newrelic-monitor="ssh -A ec2-user@54.178.183.10"
alias ssh-foryou-db='mysql -h foru-db.c2aqn1wsobic.ap-northeast-1.rds.amazonaws.com:3306  -uf0ryou -pnE411BHBaBrp'
alias ssh-ble-log="ssh ec2-user@54.65.25.24"
alias ssh-jmeter="ssh -i .ssh/ec2-vagrant.pem ec2-user@52.196.79.48"
alias ssh-japan="ssh ec2-user@52.192.87.237"

# add for ruby
alias r="/usr/bin/ruby"

# rails and sidekiq
alias restart_rails='cd /Users/feng.zhang/home/deploygate-app/deploygate/webfront; kill -9 `cat tmp/pids/server.pid`; script/sslrails s -d'
alias restart_sidekiq='sidekiqctl stop tmp/pids/sidekiq.pid 60; bundle exec sidekiq -C config/sidekiq.yml'

# Release Ctrl-Q and Ctrl-S from tty
stty stop undef
stty start undef

# source
alias sg="source ~/.bashrc-git"

# for vagrant
alias envv="envchain aws bash"
alias envstart="envchain aws bash -c 'vagrant up; vagrant gatling-rsync-auto'"
alias vagrant_s='vagrant gatling-rsync-auto'
alias vawsup="vagrant up --provider=aws"
alias vup="vagrant up"
alias vdes="vagrant destroy"
alias vpro="vagrant provision"
alias vs="vagrant status"

# swagger
alias swagger='RAILS_ENV=production rake swagger:docs'
