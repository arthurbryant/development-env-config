1. Run

node app.js

2. if error: Cannot find module 'express' 
try the following

npm install -d


Todo:
1. reportの時間を見やすいようにする

2. Add bubble
    a. D! get frequently queried keywords
    b. D! put the words into the bubble image
    c. make bubble animation(algorithm)
    d. make the page looks pretty
    e. when click the bubble it should disappear
Question:
    bubbleをクリックしたら、何か起こすの？
