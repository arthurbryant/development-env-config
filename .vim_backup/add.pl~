#!/usr/bin/perl
# 
# Author: zhangfeng cst.feng@gmail.com 2012-05-07 

use strict; 
use warnings;
use Data::Dumper;

my @TOP_BANNER_LIST = (
    [1, 2431, "img/touch_skin/piyopiyopiyo1.jpg"],
    [2, 7523, "img/touch_skin/piyopiyopiyo2.jpg"],
    [3, 2349, "img/touch_skin/piyopiyopiyo3.jpg"],
);
foreach my $item (@TOP_BANNER_LIST) {
    warn Dumper @$item[0];
}

my $hello = {};
my $test = $hello->{no};

print "undef\n" unless $test;

=pod
my @people = qw{arthur bryant wendy deuso arthur};
my %count;
$count{$_}++ foreach @people;
while((my $key, my $value) = each %count)
{
    print $key, " => ", $value, "\n";
}
foreach (my $k, my $v) (%count)
{
    print $k, "=>", $v;
}
=cut
