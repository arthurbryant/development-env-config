#!/usr/bin/perl

Package Balling;

use warnings;
use strict;

sub parse {
    my @scores
    my $total;
    foreach my $s in @score {
        $total += calc_each($s, @score);
    }
}

sub calc_each {
    my $char = shift;
    my @scores = shift;
    
}

