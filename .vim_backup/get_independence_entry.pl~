#!/script/perl

use strict;
use warnings;

use Mixi::Service::Procedure::InternalGateway;

my $entry = Mixi::Service::Procedure::InternalGateway->call(
    "jp.mixi.independence.getentry" => {
        
    }
);

printf "time == %d \n", $entry->time;

if( $entry->id > 50 ){
    printf "id == %d \n", $entry->id;
}
else{
    print "id can not be displayed \n";
}
